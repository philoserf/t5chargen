# Go build artefacts. The bare root binary is what `go build ./cmd/...`
# writes with no -o, and it was committed in the initial commit and sat
# there. Anchored, so it cannot match the cmd/t5chargen package directory.
/t5chargen
*.exe
*.test
*.out
/bin/
/dist/

# Editor
.vscode/
.idea/

# Coverage
coverage.out
coverage.html

# Source-PDF: place a local copy at docs/World Builders Handbook.pdf to use it
# while working on this repo; the file itself is not redistributed.
*.pdf

# Local worktrees for spike / experimental work
/.worktrees/

# brew bundle lockfile (regenerated locally)
Brewfile.lock.json
